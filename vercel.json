{
  "buildCommand": "npm install -g pnpm@9 && pnpm install --frozen-lockfile && pnpm -w run typecheck:libs && BASE_PATH=/ PORT=3000 NODE_ENV=production pnpm --filter @workspace/basketball-app run build && pnpm --filter @workspace/api-server run build",
  "outputDirectory": "artifacts/basketball-app/dist/public",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/server" },
    { "source": "/:path*", "destination": "/index.html" }
  ]
}